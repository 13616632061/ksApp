<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_select_adress"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray_bg"
    android:fitsSystemWindows="true"
    tools:context="com.ui.ks.SelectAdressActivity">
    <include
        layout="@layout/toolbar" android:id="@+id/toolbar_layout" />
    <ListView
        android:layout_above="@+id/btn_addadress"
        android:background="@android:color/transparent"
        android:layout_below="@+id/toolbar_layout"
        android:scrollbars="none"
        android:scrollingCache="false"
        android:animationCache="false"
        android:smoothScrollbar="true"
        android:id="@+id/list_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </ListView>
<Button
    android:layout_marginBottom="15dp"
    android:textSize="14sp"
    android:text="@string/str353"
    android:textColor="@color/white"
    android:gravity="center"
    android:layout_alignParentBottom="true"
    android:layout_centerHorizontal="true"
    android:id="@+id/btn_addadress"
    android:background="@drawable/btn_corner_orange"
    android:layout_width="180dp"
    android:layout_height="40dp" />
</RelativeLayout>
